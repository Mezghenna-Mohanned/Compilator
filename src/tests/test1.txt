MainPrgm myProgram1 ;
Var
  let x,y,z : Int ;
  let A,B : [Int;10];
  @define Const Pi : Float = 3.14 ;
  @define Const MAX : Int = 100 ;

BeginPg
{
  x := 5;
  y := 10;
  z := x + y;        /* z = 15 */

  output(x);
  output(y);
  output(z);

  A[3] := x;
  B[9] := y;
  output(A[3]);
  output(B[9]);

  /* Check out-of-bounds (should produce error if we handle it strictly) */
  A[10] := 99;  <!- This index is out of bounds -!>

  /* Constant re-assignment (should cause error if the code checks properly) */
  Pi := 4.0; <!- Not allowed, Pi is const -!>

  /* Simple if/else */
  if (z > 10) then {
    output("z > 10");
  } else {
    output("z <= 10");
  }

  x := (x + y) * (z - 2);
}
EndPg ;
